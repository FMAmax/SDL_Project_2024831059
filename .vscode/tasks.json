{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build sdl3 app",
            "type": "shell",
            "command": "g++ \"$(cygpath -u '${workspaceFolder}/Task_101/task101.cpp')\" -o \"$(cygpath -u '${workspaceFolder}/Task10X.exe')\" -I \"$(cygpath -u '${workspaceFolder}/src/include')\" -L \"$(cygpath -u '${workspaceFolder}/src/lib')\" -lSDL3 -mwindows -Wall -g",
            "group": { "kind": "build", "isDefault": true },
            "problemMatcher": ["$gcc"],
            "detail": "Builds the SDL3 application using g++"
        }
    ]
}
